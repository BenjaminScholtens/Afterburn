# force (!) so these win over the SPA catch-all and static file matches
/mgmt-api/*  https://api.dev.crowdedkingdoms.com/:splat  200!

# SPA client-side routes (must be last)
/*           /index.html  200
